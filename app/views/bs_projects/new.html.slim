.card.border-primary
  .card-body
    h1.card-title = t('action.new', name: BsProject.model_name.human)
    == render 'form'

- content_for :localnavi do
  ul.navbar-nav.flex-column.mr-auto
    li = link_to 'Back', bs_projects_path, class: "btn btn-secondary pt-side-btn"
